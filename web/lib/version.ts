// Auto-updated by sync_version.ps1 at the repo root -- do not hand-edit
// these two values, they'll just get overwritten on the next run.
//
// APP_VERSION tracks the itch.io build number ("1.0.<build>"), refreshed
// every time deploy_to_itch.ps1 pushes a new build.
//
// CUR_VERSION mirrors cur/VERSION.md's "Current corpus version" line --
// the cur/ submodule is maintained by a separate team/repo, so this file
// is what keeps the number shown here from silently drifting out of sync
// with what that submodule actually says.
export const APP_VERSION = "1.0.11";
export const CUR_VERSION = "1.2.0";
